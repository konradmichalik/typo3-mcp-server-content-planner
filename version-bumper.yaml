filesToModify:
  - path: composer.json
    patterns:
      - '"version": "{%version%}"'
  - path: ext_emconf.php
    patterns:
      - "'version' => '{%version%}'"

releaseOptions:
  commitMessage: 'release: {%version%}'
  createTag: true
  tagName: '{%version%}'
